general {
    # Lock screen with Hyprlock
    lock_cmd = hyprlock

    # Run before system suspend (can also lock here just in case)
    before_sleep_cmd = hyprlock

    # Resume command — wake up screen, etc.
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 300
    on-timeout = hyprlock
}

